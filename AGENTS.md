# Physical Memory V2 phase

- Source `./env.sh` before Android commands. Use the verified shared toolchain; do not change global Java or shell settings.
- Build with the pinned Gradle Wrapper. Domain logic must remain independent of Android speech APIs and other input devices.
- Persist records through RoomItemRepository and ItemDao transactions. Preserve identity and createdAt when updating a name. Do not use destructive migration fallback.
- V2 replaces the active command path with Qwen3-1.7B local NLU → editable draft → explicit confirmation → deterministic Room transaction. Preserve working ASR; no cloud or automatic fuzzy/alias correction. Item owns the only location; InventoryUnit has no location. NLU cannot access repository, DAO or entity types. See docs/v2_implementation_plan.md.
- Only Final can enter the shared command path. Qwen uses real Listening -> Recognizing -> Final, with manual stop and no fake Partial. Preserve per-session deduplication and cancellation guards.
- The user-requested Items page lists all stored Items, including zero stock. Manual card edits require explicit save and update by existing Item ID, preserving IDs/createdAt and rejecting duplicate names or stale snapshots. Never reuse NLU draft name rebinding as an existing-item rename. Inventory deletion remains one selected unit plus confirmation.
- The user-requested adjustment page also stages new inventory counts and per-unit dates. Each existing-unit removal must be explicitly selected and confirmed, can be undone, and only commits with Save alongside the other edits in one transaction. Cancel/back discards all staged changes; zero stock retains the Item.
- Per the current user task, reuse one Qwen recognizer per engine instance; release each stream and AudioRecord after its utterance. Serialize decode and recognizer release; cancel suppresses late native results, and clear releases the cached model after in-flight native work returns. Debug raw WAV persistence is opt-in and unavailable in release mode.
- Qwen model files are deployed separately to app-specific storage with the pinned manifest and app-UID checksum verification (internal files by default; existing external emulator deployment remains compatible); do not put the large model in the APK or ordinary JVM tests. Use scripts/setup-qwen3-model.py and scripts/deploy-qwen3-model.py --serial <device>.
- Run `./gradlew clean test lint assembleDebug build` for local validation. In this OPPO phase, use selected instrumentation classes with isolated databases for Room and Compose acceptance.
- Current sole primary development and acceptance target: OPPO Find X8s, per the user's latest task. Discover the serial using adb and verify manufacturer/market name; do not hard-code a serial in app code. Use `scripts/select-primary-device.py` for development commands. Do not test Xiaomi, emulators, or other phones in this phase. Historical reports are historical evidence only.
- The user authorized GitHub upload after the OPPO phase. Git commits and pushes for this project are now allowed. Keep model weights, recordings, databases, credentials, local environment files and build outputs out of Git. Restore pinned dependencies with scripts/setup-dependencies.py.
- UI acceptance is a fixture that stores/updates 钥匙 and expects 护照 to be absent; use a dedicated test install, not a device containing user records. Database tests use isolated in-memory or explicitly named temporary test databases.
- On a device with user records, do NOT run connectedDebugAndroidTest: the local AGP teardown uninstalls the app and its data. Use install -r and explicitly selected instrumentation classes. SpeechPermissionUiTest requires a dedicated emulator with RECORD_AUDIO revoked/reset before launching instrumentation; never reset a user's phone permissions for this fixture.
- Keep observed physical-phone microphone results distinct from Fake UI and official WAV inference results. Do not claim acoustic speech-end latency from a last-token timestamp estimate.
